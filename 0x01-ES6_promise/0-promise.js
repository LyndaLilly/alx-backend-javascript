export default function getResponseFromAPI() {
  return new Promise(() => {
    // this returns new Promise(function(resolve, reject)
    // this resolves data or anyither thing
    // for successful response the resolve can handle the data retr
    // for unsuccessful the reject function is called
  });
}
